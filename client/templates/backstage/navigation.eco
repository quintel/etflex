<% for item in @items: %>
  <li class="<%= item.key %>">
    <<%- item.openingTag %> title="<%= item.name %>">

      <% if @useIcons: %>
        <span class="icon"><%= item.name %></span>
      <% else: %>
        <%= item.name %>
      <% end %>

    </<%- item.closingTag %>>
  </li>
<% end %>
